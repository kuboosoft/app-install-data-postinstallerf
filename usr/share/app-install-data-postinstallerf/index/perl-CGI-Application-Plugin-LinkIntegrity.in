#!/bin/sh
Name='<b>perl-CGI-Application-Plugin-LinkIntegrity</b>'
Comment='<span size="xx-large">Make tamper-resistant links in CGI::Application</span>'
Comment3='
CGIApplicationPluginLinkIntegrity lets you create tamper-resistant
links within your CGIApplication project. When you create an URL with
link, a check-sum is added to the URL. The check-sum is a (cryptographic) hash
of the URL, plus a secret string known only to the server.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/CGI-Application-Plugin-LinkIntegrity/'
links within your CGI::Application project. When you create an URL with'
link, a check-sum is added to the URL. The check-sum is a (cryptographic) hash'
of the URL, plus a secret string known only to the server.'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
