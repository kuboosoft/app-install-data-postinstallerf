#!/bin/sh
Name='<b>perl-Data-Validate-Domain</b>'
Comment='<span size="xx-large">Domain validation methods Perl module</span>'
Comment3='
This module collects domain validation routines to make input validation, and
untainting easier and more readable.

All functions return an untainted value if the test passes, and undef if it
fails. This means that you should always check for a defined status explicitly.
Dont assume the return will be true. (e.g. is_username(0))
The value to test is always the first (and often only) argument.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/Data-Validate-Domain'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
