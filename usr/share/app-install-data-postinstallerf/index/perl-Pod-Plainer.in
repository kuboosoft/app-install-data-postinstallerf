#!/bin/sh
Name='<b>perl-Pod-Plainer</b>'
Comment='<span size="xx-large">Perl extension for converting Pod to old-style Pod</span>'
Comment3='
PodPlainer uses PodParser which takes Pod with the (new) C<< .. >>
constructs and returns the old(er) style with just C<>; < and > are
replaced by E<lt> and E<gt>.
This can be used to pre-process Pod before using tools which do not
recognize the new style Pods.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/Pod-Plainer/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
