#!/bin/sh
Name='<b>grep</b>'
Comment='<span size="xx-large">Pattern matching utilities</span>'
Comment3='
The GNU versions of commonly used grep utilities. Grep searches through
textual input for lines which contain a match to a specified pattern and then
prints the matching lines. GNUs grep utilities include grep, egrep and fgrep.

GNU grep is needed by many scripts, so it shall be installed on every system.
'
License='GPLv3+'
Screenshot=''
Url='http://www.gnu.org/software/grep/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
