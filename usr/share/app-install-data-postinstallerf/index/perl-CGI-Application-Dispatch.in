#!/bin/sh
Name='<b>perl-CGI-Application-Dispatch</b>'
Comment='<span size="xx-large">Dispatch requests to CGI::Application based objects</span>'
Comment3='
This module provides a way (as a mod_perl handler or running under vanilla
CGI) to look at the path (as returned by dispatch_path) of the incoming
request, parse off the desired module and its run mode, create an instance
of that module and run it.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/CGI-Application-Dispatch/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
