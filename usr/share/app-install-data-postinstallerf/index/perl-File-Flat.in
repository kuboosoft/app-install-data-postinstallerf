#!/bin/sh
Name='<b>perl-File-Flat</b>'
Comment='<span size="xx-large">Implements a flat filesystem</span>'
Comment3='
FileFlat implements a flat filesystem. A flat filesystem is a filesystem
in which directories do not exist. It provides an abstraction over any
normal filesystem which makes it appear as if directories do not exist.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/File-Flat/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
