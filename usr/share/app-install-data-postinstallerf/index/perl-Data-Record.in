#!/bin/sh
Name='<b>perl-Data-Record</b>'
Comment='<span size="xx-large">"split" on steroids</span>'
Comment3='
Sometimes we need data split into records and a simple split on the
input record separator ($/) or some other value fails because the values
were splitting on may allowed in other parts of the data. Perhaps
theyre quoted. Perhaps theyre embedded in other data which should not
be split up.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/Data-Record/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
