#!/bin/sh
Name='<b>perl-HTTP-Parser</b>'
Comment='<span size="xx-large">Parse HTTP/1.1 request into HTTP::Request/Response object</span>'
Comment3='
This is an HTTP request parser. It takes chunks of text as received and
returns a hint as to what is required, or returns the HTTPRequest when
a complete request has been read. HTTP/1.1 chunking is supported. It dies
if it finds an error.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/HTTP-Parser/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
