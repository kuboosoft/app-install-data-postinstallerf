#!/bin/sh
Name='<b>perl-JSON-PP</b>'
Comment='<span size="xx-large">JSON::XS compatible pure-Perl module</span>'
Comment3='
JSONXS is the fastest and most proper JSON module on CPAN. It is written by
Marc Lehmann in C, so must be compiled and installed in the used environment.

JSONPP is a pure-Perl module and is compatible with JSONXS.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/JSON-PP/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
