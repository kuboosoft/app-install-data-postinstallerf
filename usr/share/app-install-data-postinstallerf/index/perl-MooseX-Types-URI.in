#!/bin/sh
Name='<b>perl-MooseX-Types-URI</b>'
Comment='<span size="xx-large">URI related types and coercions for Moose</span>'
Comment3='
This package provides Moose types for fun (and profit) with the URI classes.

It has slightly DWIMier types than the the URI classes have due to
implementation details, so the types should be more forgiving when
ducktyping will work anyway (e.g. URIWithBase does not inherit URI).
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/MooseX-Types-URI'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
