#!/bin/sh
Name='<b>perl-constant-defer</b>'
Comment='<span size="xx-large">Constant subs with deferred value calculation</span>'
Comment3='
constantdefer creates a subroutine which on the first call runs given
code to calculate its value, and on the second and subsequent calls just
returns that value, like a constant. The value code is discarded once run,
allowing it to be garbage collected.
'
License='GPLv3+'
Screenshot=''
Url='http://search.cpan.org/dist/constant-defer/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
