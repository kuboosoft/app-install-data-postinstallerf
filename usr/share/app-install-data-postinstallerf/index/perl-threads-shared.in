#!/bin/sh
Name='<b>perl-threads-shared</b>'
Comment='<span size="xx-large">Perl extension for sharing data structures between threads</span>'
Comment3='
By default, variables are private to each thread, and each newly created
thread gets a private copy of each existing variable. This module allows
you to share variables across different threads (and pseudo-forks on
Win32). It is used together with the threads module.
'
License='GPL+ or Artistic'
Screenshot=''
Url='http://search.cpan.org/dist/threads-shared/'
export Comment
export Name
export Comment3
export License
export Screenshot
export Url
